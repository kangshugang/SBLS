'''
    python sbls.py module -n <beam width> --kb <kb_file>

    problem/
        active/
        paused/
        finished/
        discarded/
        failed/

'''

def main():

    # create model
    # save the model into a file in the active folder

    # loop

        # while active folder is not empty
            # pick one model
            # run it

        # if paused is not empty:
            # measure and filter the paused
            # move them to active
            # continue

        # if finished is not empty:
            # done

        # move N discarded into active
            # continue

