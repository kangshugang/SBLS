
def main():

    # prepare the training data for each decision point
    # agregate the performance measures for each solution (recursive method)

    # for each agent request, train a model with the data
    # {global_features} {local_features} {decision_features} {performance}

    # save the trained model into a sqlite database (kb)

